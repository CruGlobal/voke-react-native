jobs:
  include:
    - stage: Deploy iOS 🍏
      if: branch = master AND type = push
      language: node_js
      node_js: 10
      os: osx
      osx_image: xcode11.3
      install:
        - bundle install
        - npm install
      script: npm run ios:beta
